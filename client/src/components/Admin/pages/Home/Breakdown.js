import React from "react";

const Breakdown = ({ sectionClasses }) => {
  return (
    <div className={`${sectionClasses} w-1/3`}>
      <h1 className="">Hello</h1>
    </div>
  );
};

export default Breakdown;
